#include "holberton.h"

/**
 * mul - multiplies two number inputs
 * @a: first number to multiply from user input
 * @b: second number to multiply from user input
 * Return: the number after multiplying both variables
 */
int mul(int a, int b)
{
	int n;

	n = a * b;
	return (n);
}
